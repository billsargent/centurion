# Node
node_modules/
dist/

# Disk images (large binary data — keep out of git)
server/disks/*.IMG
server/disks/*.BIN
server/disks/*.FFI

# Logs / OS junk
*.log
.DS_Store
Thumbs.db

# Large copyrighted manual scans (kept locally, not in git)
CPU6 Programmer Manual/

# Temp extraction artifacts
tmp-*.txt
extract-pdf.py
